{
  "$schema": "../gen/schemas/mobile-schema.json",
  "identifier": "mobile",
  "description": "Extra permissions on Android and iOS. Kept out of the default file because they only exist there: declaring them everywhere would fail the desktop build on a permission whose plugin is absent. One entry, and it only grants access to the system prompt — the master key never passes through the plugin: it is sealed by the device secrets and returned by a command that triggers the prompt itself.",
  "platforms": ["android", "iOS"],
  "windows": ["main"],
  "permissions": ["biometric:default"]
}
